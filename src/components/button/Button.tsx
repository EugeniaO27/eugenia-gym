import React from "react";

const Button = () => {
  return (
    <div>
      <button
        className="px-10 py-4 rounded-full cursor-pointer text-white bg-primary-500
         hover:bg-primary-200">
        Join Now
      </button>
    </div>
  );
};

export default Button;
